# Development (Vite dev server proxies relative API routes to backend)
VITE_DEV_PROXY_TARGET=http://localhost:8000

# Production (frontend build uses direct backend URL)
VITE_API_BASE_URL=https://api.example.com
